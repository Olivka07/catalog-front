import { Card } from 'shared/ui/Card/Card';
import { ProductsList } from './ProductsList';

export const Catalog = () => {
    return (
        <section>
            <ProductsList />
        </section>
    );
};
